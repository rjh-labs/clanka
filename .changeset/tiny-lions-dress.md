---
"clanka": patch
---

Add a GitHub Copilot provider layer backed by the device auth flow and the Copilot OpenAI-compatible API.

Implement `ScriptExtraction.extractScript` to extract fenced code blocks from markdown responses.
