---
"clanka": patch
---

Fix script preprocessing for task update descriptions by escaping broken backticks in `updateTask` `description` templates (including identifier assignments), while preserving existing non-patch escapes.
